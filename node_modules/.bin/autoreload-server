#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../simple-autoreload-server/bin/autoreload" "$@"
  ret=$?
else 
  node  "$basedir/../simple-autoreload-server/bin/autoreload" "$@"
  ret=$?
fi
exit $ret
